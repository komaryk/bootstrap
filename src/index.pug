extends layouts/main

append params
	- pageTitle = "Сторінка 1"

block content
	+header
	main(role='main')
		+cta
		.container
			+about
			+services
		.container-fluid.border-bottom.border-top
			+achievements
		.container.pt-4
			+gallery
	+footer